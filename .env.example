# ── Backend environment variables ─────────────────────────────────────────────
# Comma-separated list of allowed CORS origins.
# Set this on Railway to your frontend's public URL, e.g.:
#   ALLOWED_ORIGINS=https://fight-uwu-bird-frontend.up.railway.app
# Leave unset for local development (defaults to localhost).
ALLOWED_ORIGINS=

# ── Frontend environment variables ────────────────────────────────────────────
# Full URL of the backend API service (no trailing slash).
# Set this as a Railway build variable so Vite bakes it into the bundle.
#   VITE_API_URL=https://fight-uwu-bird-backend.up.railway.app
# Leave empty for local development (Vite dev proxy handles /api → localhost:8000).
VITE_API_URL=
